"""Namespace holder so `python -m cli.redforge ...` works from the repo root.

The installable package is ``redforge`` (see the root ``pyproject.toml`` and
``cli/pyproject.toml``); this file only makes ``cli`` importable in-place.
"""
