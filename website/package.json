{
  "name": "redforge-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "RedForge marketing website — a separate project from /frontend.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "lucide-react": "^0.441.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.11",
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  },
  "comment": "Motion is implemented in-house (see src/motion). The design brief calls for GSAP + Framer Motion + Lenis; add them with `npm i gsap framer-motion lenis` when a registry is reachable and swap the in-house primitives in src/motion — the seams are documented there."
}
