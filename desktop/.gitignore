# Desktop build outputs — all regenerated by `stage-backend.py` + electron-builder.
node_modules/
release-build/
resources/
.pyinstaller/
__pycache__/

# buildResources (desktop/build/) IS tracked — it holds icon.png + entitlements.
# Only ignore stray build scratch that lands there.
build/*.spec
